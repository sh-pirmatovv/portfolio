FROM node:22-alpine

ARG APP_PATH

WORKDIR /workspace

COPY package.json pnpm-workspace.yaml turbo.json tsconfig.base.json ./
COPY apps ./apps
COPY packages ./packages

RUN corepack enable

CMD ["sh", "-c", "cd ${APP_PATH} && echo 'Install dependencies with pnpm before building this image.' && sleep infinity"]

